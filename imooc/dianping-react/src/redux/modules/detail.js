const initialState = {
  product: {
    isFetching: false,
    id: null,
  },
  relatedShop: {
    isFetching: false,
    id: null,
  }
}

const reducer = (state = {}, action) => {
  return state 
}

export default reducer